# Agentic AI POC - File Categorization

A simple LangGraph agent that categorizes and moves files between Azure Blob Storage containers using Azure OpenAI.

## Setup

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```

2. Configure `.env` with your Azure credentials

3. Run:
   ```
   python run.py
   ```
